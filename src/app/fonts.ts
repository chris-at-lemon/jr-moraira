import { Cardo, Nunito } from "next/font/google";

export const cardo = Cardo({
  subsets: ["latin"],
  display: "swap",
  weight: "700",
});

export const nunito = Nunito({
  subsets: ["latin"],
  display: "swap",
});
